<header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <%= link_to(root_path, class: "navbar-brand") do %>Bookers<% end %>

          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></sapn>
          </button>

          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <% if user_signed_in? %>
                <li class="nav-item active">
                  <%= link_to(user_path(current_user), class: "nav-link text-light mr-2") do %>Home<% end %>
                </li>
                <li class="nav-item">
                  <%= link_to(users_path, class: "nav-link text-light mr-2") do %>Users<% end %>
                </li>
                <li class="nav-item">
                  <%= link_to(books_path, class: "nav-link text-light mr-2") do %>Books<% end %>
                </li>
                <li class="nav-item">
                  <%= link_to(destroy_user_session_path, method: :delete, class: "nav-link text-light") do %>logout<% end %>
                </li>
              <% else %>
                <li class="nav-item">
                  <%= link_to(root_path, class: "nav-link text-light mr-2") do %>Home<% end %>
                </li>
                <li class="nav-item">
                  <%= link_to(home_about_path, class: "nav-link text-light mr-2") do %>About<% end %>
                </li>
                <li class="nav-item">
                  <%= link_to(new_user_registration_path, class: "nav-link text-light mr-2") do %>sign up<% end %>
                </li>
                <li class="nav-item">
                  <%= link_to(new_user_session_path, class: "nav-link text-light") do %>login<% end %>
                </li>
              <% end %>
            </ul>
          </div>

        </div>
      </nav>

    </header>